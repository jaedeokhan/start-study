# 동시성 보고서

## 📋 목차
1. [문제 식별](#1-문제-식별)
2. [동시성 이슈 분석](#2-동시성-이슈-분석)
3. [해결 방안 선정](#3-해결-방안-선정)
4. [결론](#4-결론)

---

## 1. 문제 식별

### 1.1 서비스 개요
E-commerce 시스템에서 다수의 사용자가 동시에 다음 작업을 수행합니다:
- **쿠폰 발급**: 선착순 이벤트 쿠폰 발급
- **재고 차감**: 상품 주문 시 재고 감소
- **포인트 충전**: 사용자 포인트 잔액 증가
- **포인트 사용**: 주문 결제 시 포인트 차감

### 1.2 동시성 문제가 발생하는 시나리오

#### 시나리오 1: 쿠폰 발급
```
상황: 선착순 100명 한정 쿠폰 이벤트
문제: 200명이 동시에 발급 시도

시간  트랜잭션 A              트랜잭션 B              결과
T1    SELECT issued=99       -
T2    -                      SELECT issued=99       둘 다 발급 가능으로 판단
T3    UPDATE issued=100      -
T4    -                      UPDATE issued=101      ❌ 초과 발급!
```

#### 시나리오 2: 재고 차감
```
상황: 재고 1개 남은 상품
문제: 2명이 동시에 구매 시도

시간  사용자 A               사용자 B               결과
T1    SELECT stock=1         -
T2    -                      SELECT stock=1         둘 다 구매 가능으로 판단
T3    UPDATE stock=0         -
T4    -                      UPDATE stock=-1        ❌ 재고 음수!
```

#### 시나리오 3: 포인트 충전

**실제 일반적인 케이스 - 여러 사용자의 독립적인 충전**
```
상황: 여러 사용자가 각자의 포인트 충전
- 사용자 A: 자신의 포인트 충전 (user_id=1)
- 사용자 B: 자신의 포인트 충전 (user_id=2)

시간  사용자 A (id=1)        사용자 B (id=2)        결과
T1    SELECT balance WHERE id=1
T2    -                      SELECT balance WHERE id=2
T3    UPDATE balance WHERE id=1
T4    -                      UPDATE balance WHERE id=2    ✅ 충돌 없음 (다른 레코드)
```

**예외 케이스 - 동일 사용자의 중복 요청**
```
상황: 한 사용자가 10,000원 잔액 보유
문제: 5,000원 충전 요청을 2번 동시에 전송 (네트워크 지연/재시도, UI 버그 등)

시간  요청 A                 요청 B                 결과
T1    SELECT balance=10000   -
T2    -                      SELECT balance=10000   같은 잔액 읽음
T3    UPDATE balance=15000   -
T4    -                      UPDATE balance=15000   ❌ 5,000원 누락!

⚠️ 실무에서는 매우 드문 케이스 (중복 요청 방지 로직 필요)
```

#### 시나리오 4: 포인트 사용 (주문)

**실제 일반적인 케이스 - 여러 사용자의 독립적인 주문**
```
상황: 여러 사용자가 각자 주문
- 사용자 A: 자신의 포인트로 결제 (user_id=1)
- 사용자 B: 자신의 포인트로 결제 (user_id=2)

시간  사용자 A (id=1)        사용자 B (id=2)        결과
T1    SELECT balance WHERE id=1
T2    -                      SELECT balance WHERE id=2
T3    UPDATE balance WHERE id=1
T4    -                      UPDATE balance WHERE id=2    ✅ 충돌 없음 (다른 레코드)
```

**예외 케이스 - 동일 사용자의 중복 주문**
```
상황: 한 사용자가 50,000원 잔액 보유
문제: 30,000원 주문을 2번 동시에 시도 (결제 버튼 중복 클릭 등)

시간  주문 A                 주문 B                 결과
T1    SELECT balance=50000   -
T2    -                      SELECT balance=50000   같은 잔액 읽음
T3    UPDATE balance=20000   -
T4    -                      UPDATE balance=20000   ❌ 30,000원 누락!

⚠️ 실무에서는 매우 드문 케이스 (중복 제출 방지 로직 필요)
```

---

## 2. 동시성 이슈 분석

### 2.1 발생 원인

#### Race Condition (경쟁 상태)
- **정의**: 여러 스레드가 공유 자원에 동시 접근하여 실행 순서에 따라 결과가 달라지는 현상
- **발생 패턴**: Check-Then-Act, Read-Modify-Write

#### Lost Update (갱신 손실)
- **정의**: 한 트랜잭션의 업데이트가 다른 트랜잭션에 의해 덮어씌워지는 현상
- **영향**: 데이터 무결성 손실

### 2.2 시나리오별 충돌 확률 및 영향도 분석

| 시나리오 | 충돌 확률 | 비즈니스 영향도 | 데이터 중요도 | 발생 빈도 |
|---------|----------|---------------|-------------|----------|
| **쿠폰 발급** | 🔴 매우 높음 | 🔴 Critical | 법적 리스크 | 이벤트 시 |
| **재고 차감** | 🔴 높음 | 🔴 Critical | 과다 판매 | 인기 상품 |
| **포인트 충전** | 🟡 낮음 | 🔴 Critical | 금전 손실 | 드묾 |
| **포인트 사용** | 🟡 낮음 | 🔴 Critical | 금전 손실 | 드묾 |

### 2.3 각 시나리오별 특성 분석

#### 쿠폰 발급
- **충돌 확률**: 매우 높음 (이벤트 시작 순간 트래픽 집중)
- **특성**: 선착순, 한정 수량
- **재시도 필요성**: 불필요 (이미 마감되면 의미 없음)

#### 재고 차감
- **충돌 확률**: 높음 (인기 상품, 세일 기간)
- **특성**: 물리적 제약, 음수 불가
- **재시도 필요성**: 불필요 (품절 시 의미 없음)

#### 포인트 충전
- **일반 케이스 (여러 사용자)**:
  - 충돌 없음 (각 사용자는 자신의 포인트만 조작)
  - 레코드 단위 격리로 충분
- **예외 케이스 (동일 사용자 중복 요청)**:
  - 충돌 확률: 매우 낮음 (네트워크 이슈, UI 버그 등)
  - 특성: 금전 관련, 누락 시 손실
  - 재시도 필요성: 높음 (성공해야 함)

#### 포인트 사용
- **일반 케이스 (여러 사용자)**:
  - 충돌 없음 (각 사용자는 자신의 포인트만 조작)
  - 레코드 단위 격리로 충분
- **예외 케이스 (동일 사용자 중복 주문)**:
  - 충돌 확률: 매우 낮음 (결제 버튼 중복 클릭 등)
  - 특성: 금전 관련, 잔액 부족 체크 필요
  - 재시도 필요성: 높음 (결제 완료 필요)

---

## 3. 해결 방안 선정

### 3.1 동시성 제어 메커니즘 비교

#### 비관적 락 (Pessimistic Lock)

**동작 방식**:
```sql
-- 조회 시점에 즉시 락 획득
SELECT * FROM products WHERE id = 1 FOR UPDATE;
-- 다른 트랜잭션은 대기
UPDATE products SET stock = stock - 1 WHERE id = 1;
COMMIT;  -- 락 해제
```

**장점**:
- ✅ 데이터 정합성 100% 보장
- ✅ 구현이 단순 (`@Lock` 애노테이션)
- ✅ 충돌 확률이 높을 때 효율적

**단점**:
- ❌ 락 대기로 인한 성능 저하
- ❌ 데드락 발생 가능성
- ❌ 동시 처리량 제한

**적합한 상황**:
- 충돌 확률이 높음
- 데이터 정합성이 최우선
- 실패 시 명확한 메시지 전달 가능

---

#### 낙관적 락 (Optimistic Lock)

**동작 방식**:
```sql
-- 조회 시 version 함께 읽음
SELECT id, stock, version FROM products WHERE id = 1;  -- version=5

-- 업데이트 시 version 체크
UPDATE products
SET stock = stock - 1, version = 6
WHERE id = 1 AND version = 5;  -- version 일치 시만 성공

-- version 불일치 시 OptimisticLockException 발생 → 재시도
```

**장점**:
- ✅ 락이 없어 성능 우수
- ✅ 데드락 위험 없음
- ✅ 읽기 작업에 영향 없음

**단점**:
- ❌ 충돌 시 재시도 필요
- ❌ 재시도 로직 구현 필요
- ❌ 충돌 확률이 높으면 오히려 비효율

**적합한 상황**:
- 충돌 확률이 낮음
- 읽기 작업이 많음
- 재시도 가능한 작업

---

### 3.2 시나리오별 최적 전략 선정

#### 쿠폰 발급 → **비관적 락** ✅

**선정 이유**:
충돌 확률이 높고 순차 처리가 매우 중요하기에 비관적 락 선택

**결론**:
- 충돌 확률이 극도로 높아 낙관적 락 사용 시 대부분 재시도 발생
- 선착순 특성상 순차 처리가 더 공정
- **비관적 락이 최적**

---

#### 재고 차감 → **비관적 락** ✅

**선정 이유** :
충돌 확률이 높고 데이터 중요도가 높기에 비관적 락 선택

**결론**:
- 재고는 절대 음수가 되어서는 안 됨
- 인기 상품은 충돌 확률이 매우 높음
- **비관적 락이 최적**

---

#### 포인트 충전 → **낙관적 락** ✅

**선정 이유**:
데이터의 중요도는 높지만 실제 충돌 확률이 극히 낮기에 성능을 위해서 낙관적 락 선택

**상황 분석**:
- **일반 케이스**: 여러 사용자가 각자의 포인트 충전 → 서로 다른 레코드 → 충돌 없음
- **예외 케이스**: 동일 사용자의 중복 요청 → 동일 레코드 → 충돌 가능 (매우 드묾)

**결론**:
- 대부분의 경우 충돌이 발생하지 않음 (다른 사용자 = 다른 레코드)
- 예외적인 중복 요청도 재시도로 해결 가능
- 비관적 락 사용 시 불필요한 성능 저하
- **낙관적 락이 최적**

**추가 고려사항**:
- 중복 요청 방지: 클라이언트에서 버튼 비활성화, 멱등성 키 사용 등

---

#### 포인트 사용 (주문) → **낙관적 락** ✅

**선정 이유**:
데이터의 중요도는 높지만 실제 충돌 확률이 극히 낮기에 성능을 위해서 낙관적 락 선택

**상황 분석**:
- **일반 케이스**: 여러 사용자가 각자 주문 → 서로 다른 레코드 → 충돌 없음
- **예외 케이스**: 동일 사용자의 중복 주문 → 동일 레코드 → 충돌 가능 (매우 드묾)

**결론**:
- 대부분의 경우 충돌이 발생하지 않음 (다른 사용자 = 다른 레코드)
- 주문 트랜잭션이 길어 비관적 락 사용 시 성능 저하 큼
- 예외적인 중복 주문도 재시도로 해결 가능
- **낙관적 락이 최적**

**추가 고려사항**:
- 중복 주문 방지: 주문 ID 기반 멱등성 처리, 클라이언트 중복 제출 방지

---

### 3.3 최종 전략 요약

| 기능 | 선택한 락 | 핵심 이유 |
|-----|---------|----------|
| 쿠폰 발급 | 비관적 락 | 높은 충돌률, 선착순 특성 |
| 재고 차감 | 비관적 락 | 높은 충돌률, 음수 절대 불가 |
| 포인트 충전 | 낙관적 락 | 낮은 충돌률, 성능 중요 |
| 포인트 사용 | 낙관적 락 | 낮은 충돌률, 성능 중요 |

---

## 4. 결론

**READ_COMMITTED 선택 이유**:
- Dirty Read 방지 (커밋되지 않은 데이터 읽기 방지)
- 비관적 락/낙관적 락과 조합 시 충분한 동시성 제어
- MySQL InnoDB 기본값과 일치
- 과도한 락 방지로 성능 우수

### 4.1 전략 요약

| 기능 | 동시성 제어 방식 | 핵심 근거 |
|-----|---------------|----------|
| **쿠폰 발급** | 비관적 락 | • 매우 높은 충돌률 (이벤트 시작 순간 폭증)<br>• 선착순 특성상 순차 처리가 공정<br>• 재시도 불필요 (이미 마감 시 의미 없음) |
| **재고 차감** | 비관적 락 | • 높은 충돌률 (인기 상품, 세일)<br>• 재고 음수 절대 불가<br>• 재시도 불필요 (품절 시 의미 없음) |
| **포인트 충전** | 낙관적 락 + 재시도 | • 일반 케이스: 충돌 없음 (여러 사용자 = 다른 레코드)<br>• 예외 케이스: 극히 낮은 충돌률 (동일 사용자 중복 요청)<br>• 성능 이점 (락 대기 없음)<br>• 재시도로 충분히 해결 가능 |
| **포인트 사용** | 낙관적 락 + 재시도 | • 일반 케이스: 충돌 없음 (여러 사용자 = 다른 레코드)<br>• 예외 케이스: 극히 낮은 충돌률 (동일 사용자 중복 주문)<br>• 긴 트랜잭션에서 성능 중요<br>• 재시도로 충분히 해결 가능 |

### 4.2 핵심 설계 원칙

1. **충돌 확률 기반 선택**
   - 높은 충돌률 → 비관적 락 (순차 처리)
   - 낮은 충돌률 → 낙관적 락 (재시도)

2. **데이터 정합성 최우선**
   - 모든 시나리오에서 데이터 정합성 100% 보장
   - 금전 관련 데이터는 절대 누락 불가

3. **성능과 안정성의 균형**
   - 충돌이 잦은 작업: 안정성 우선 (비관적 락)
   - 충돌이 드문 작업: 성능 우선 (낙관적 락)

### 4.3 기대 효과

#### 비관적 락 (쿠폰, 재고)
- ✅ 데이터 정합성 100% 보장
- ✅ 초과 발급/판매 완전 차단
- ✅ 법적 리스크 제거
- ⚠️ 처리량 약 40% 감소 (허용 가능)

#### 낙관적 락 (포인트)
- ✅ 데이터 정합성 100% 보장 (재시도로)
- ✅ 락 대기 없어 성능 우수
- ✅ 충돌 시 자동 재시도로 사용자 경험 유지
- ✅ 긴 트랜잭션에서도 높은 동시 처리량
